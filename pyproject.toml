[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cmake>=3.18",
    "pybind11[global]==2.13.1"
]

build-backend = "setuptools.build_meta"

[project]
name = "gbrl"
version = "1.1.7"
description = "Gradient Boosted Trees for RL"
readme = { file = "README.md", content-type = "text/markdown" }
authors = [
    {name = "Benjamin Fuhrer", email = "bfuhrer@nvidia.com"},
    {name = "Chen Tessler", email = "ctessler@nvidia.com"},
    {name = "Gal Dalal", email = "galal@nvidia.com"}
]
# Note: pybind11 appears in both build-system.requires (for C++ extension compilation)
# and dependencies (for runtime headers). This is intentional.
dependencies = [
    "pybind11[global]==2.13.1",
    "numpy>=1.21.0",
    "cmake>=3.18",
    "torch>=1.13.1",
    "scikit-learn>=1.5.0",
    "scipy>=1.7.0",
    "shap>=0.40.0",
    "matplotlib>=3.5.0",
    "fonttools>=4.60.2",
    "filelock>=3.20.1",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: Other/Proprietary License",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence"
]

dynamic = ["license"]

requires-python = ">=3.9"

[project.urls]
Homepage = "https://github.com/NVlabs/gbrl"
Documentation = "https://nvlabs.github.io/gbrl/"
Repository = "https://github.com/NVlabs/gbrl"

[tool.poetry]
name = "gbrl"
version = "1.1.7"
description = "Gradient Boosted Trees for RL"
authors = ["Benjamin Fuhrer <bfuhrer@nvidia.com>", "Chen Tessler <ctessler@nvidia.com>", "Gal Dalal <galal@nvidia.com>"]
readme = "README.md"
packages = [{include = "gbrl"}]

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
pybind11 = {version = "2.13.1", extras = ["global"]}
numpy = ">=1.21.0"
cmake = ">=3.18"
torch = ">=2.8.0"
scikit-learn = ">=1.5.0"
scipy = ">=1.7.0"
shap = ">=0.40.0"
matplotlib = ">=3.5.0"
jinja2 = ">=3.1.6"
setuptools = ">=78.1.1"
Pillow = ">=11.3.0"
fonttools = ">=4.60.2"
filelock = ">=3.20.1"


[tool.poetry.group.dev]
[tool.poetry.group.dev.dependencies]
pytest = "*"

